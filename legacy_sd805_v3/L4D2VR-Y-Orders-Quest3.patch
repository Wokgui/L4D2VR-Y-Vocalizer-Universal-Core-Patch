diff --git a/L4D2VR/SteamVRActionManifest/action_manifest.json b/L4D2VR/SteamVRActionManifest/action_manifest.json
index 3b3b5d5..2474f2a 100644
--- a/L4D2VR/SteamVRActionManifest/action_manifest.json
+++ b/L4D2VR/SteamVRActionManifest/action_manifest.json
@@ -134,6 +134,11 @@
 		{
 			"name": "/actions/main/in/Pause",
 			"type": "boolean"
+		},
+		{
+			"name": "/actions/orders/in/Hold",
+			"type": "boolean",
+			"requirement": "optional"
 		}
 	],
 
@@ -145,6 +150,10 @@
 		{
 		  "name": "/actions/base",
 		  "usage": "hidden"
+		},
+		{
+		  "name": "/actions/orders",
+		  "usage": "hidden"
 		}
 	],
 
@@ -174,7 +183,9 @@
 			"/actions/main/in/Spray" : "Spray",
 			"/actions/main/in/Scoreboard" : "Show Scoreboard",
 			"/actions/main/in/ShowHUD" : "Show HUD",
-			"/actions/main/in/Pause" : "Pause"
+			"/actions/main/in/Pause" : "Pause",
+			"/actions/orders" : "Orders Hold",
+			"/actions/orders/in/Hold" : "Orders Hold"
 		}
 	]
 }
diff --git a/L4D2VR/SteamVRActionManifest/bindings_oculus_touch.json b/L4D2VR/SteamVRActionManifest/bindings_oculus_touch.json
index ca68418..98b68ba 100644
--- a/L4D2VR/SteamVRActionManifest/bindings_oculus_touch.json
+++ b/L4D2VR/SteamVRActionManifest/bindings_oculus_touch.json
@@ -36,13 +36,23 @@
          ],
          "sources" : []
       },
+      "/actions/orders" : {
+         "sources" : [
+			{
+               "inputs" : {
+                  "click" : {
+                     "output" : "/actions/orders/in/Hold"
+                  }
+               },
+               "mode" : "button",
+               "path" : "/user/hand/left/input/y"
+            }
+         ]
+      },
       "/actions/main" : {
          "sources" : [
             {
                "inputs" : {
-                  "click" : {
-                     "output" : "/actions/main/in/ResetPosition"
-                  },
                   "position" : {
                      "output" : "/actions/main/in/Walk"
                   }
@@ -185,15 +195,6 @@
                },
                "mode" : "button",
                "path" : "/user/hand/left/input/x"
-            },
-			{
-               "inputs" : {
-                  "click" : {
-                     "output" : "/actions/main/in/Pause"
-                  }
-               },
-               "mode" : "button",
-               "path" : "/user/hand/left/input/y"
             },
             {
                "inputs" : {
diff --git a/L4D2VR/vr.cpp b/L4D2VR/vr.cpp
index bb53309..c8b39ab 100644
--- a/L4D2VR/vr.cpp
+++ b/L4D2VR/vr.cpp
@@ -135,10 +135,17 @@ int VR::SetActionManifest(const char *fileName)
     m_Input->GetActionHandle("/actions/main/in/Scoreboard", &m_Scoreboard);
     m_Input->GetActionHandle("/actions/main/in/ShowHUD", &m_ShowHUD);
     m_Input->GetActionHandle("/actions/main/in/Pause", &m_Pause);
+    m_Input->GetActionHandle("/actions/orders/in/Hold", &m_OrdersMenu);
 
     m_Input->GetActionSetHandle("/actions/main", &m_ActionSet);
+    m_Input->GetActionSetHandle("/actions/orders", &m_OrdersActionSet);
+    m_Input->GetInputSourceHandle("/user/hand/left", &m_LeftHandInputSource);
     m_ActiveActionSet = {};
     m_ActiveActionSet.ulActionSet = m_ActionSet;
+    m_ActiveOrdersActionSet = {};
+    m_ActiveOrdersActionSet.ulActionSet = m_OrdersActionSet;
+    m_ActiveOrdersActionSet.ulRestrictedToDevice = m_LeftHandInputSource;
+    m_ActiveOrdersActionSet.nPriority = vr::k_nActionSetOverlayGlobalPriorityMax;
 
     return 0;
 }
@@ -178,6 +185,8 @@ void VR::Update()
         ProcessMenuInput();
     else
         ProcessInput();
+
+    ProcessOrdersMenuInput();
 }
 
 void VR::CreateVRTextures()
@@ -369,7 +378,8 @@ void VR::GetPoses()
 void VR::UpdatePosesAndActions() 
 {
     vr::VRCompositor()->WaitGetPoses(m_Poses, vr::k_unMaxTrackedDeviceCount, NULL, 0);
-    m_Input->UpdateActionState(&m_ActiveActionSet, sizeof(vr::VRActiveActionSet_t), 1);
+    vr::VRActiveActionSet_t activeActionSets[2] = {m_ActiveActionSet, m_ActiveOrdersActionSet};
+    m_Input->UpdateActionState(activeActionSets, sizeof(vr::VRActiveActionSet_t), 2);
 }
 
 void VR::GetViewParameters() 
@@ -401,6 +411,40 @@ bool VR::PressedDigitalAction(vr::VRActionHandle_t &actionHandle, bool checkIfAc
     return false;
 }
 
+void VR::ProcessOrdersMenuInput()
+{
+    if (!m_Game->m_EngineClient->IsInGame())
+    {
+        if (m_OrdersMenuOpen)
+        {
+            m_Game->ClientCmd_Unrestricted("-mouse_menu");
+            m_OrdersMenuOpen = false;
+        }
+        return;
+    }
+
+    vr::InputDigitalActionData_t actionData = {};
+    const vr::EVRInputError result = m_Input->GetDigitalActionData(
+        m_OrdersMenu,
+        &actionData,
+        sizeof(actionData),
+        m_LeftHandInputSource);
+
+    if (result != vr::VRInputError_None || !actionData.bActive)
+        return;
+
+    if (actionData.bState && !m_OrdersMenuOpen)
+    {
+        m_Game->ClientCmd_Unrestricted("+mouse_menu Orders");
+        m_OrdersMenuOpen = true;
+    }
+    else if (!actionData.bState && m_OrdersMenuOpen)
+    {
+        m_Game->ClientCmd_Unrestricted("-mouse_menu");
+        m_OrdersMenuOpen = false;
+    }
+}
+
 bool VR::GetAnalogActionData(vr::VRActionHandle_t &actionHandle, vr::InputAnalogActionData_t &analogDataOut)
 {
     vr::EVRInputError result = m_Input->GetAnalogActionData(actionHandle, &analogDataOut, sizeof(analogDataOut), vr::k_ulInvalidInputValueHandle);
diff --git a/L4D2VR/vr.h b/L4D2VR/vr.h
index 11c8fea..54704bc 100644
--- a/L4D2VR/vr.h
+++ b/L4D2VR/vr.h
@@ -139,6 +139,9 @@ public:
 	// action set
 	vr::VRActionSetHandle_t m_ActionSet;
 	vr::VRActiveActionSet_t m_ActiveActionSet;
+	vr::VRActionSetHandle_t m_OrdersActionSet;
+	vr::VRActiveActionSet_t m_ActiveOrdersActionSet;
+	vr::VRInputValueHandle_t m_LeftHandInputSource;
 
 	// actions
 	vr::VRActionHandle_t m_ActionJump;
@@ -164,6 +167,7 @@ public:
 	vr::VRActionHandle_t m_Scoreboard;
 	vr::VRActionHandle_t m_ShowHUD;
 	vr::VRActionHandle_t m_Pause;
+	vr::VRActionHandle_t m_OrdersMenu;
 
 	TrackedDevicePoseData m_HmdPose;
 	TrackedDevicePoseData m_LeftControllerPose;
@@ -182,6 +186,7 @@ public:
 	float m_HudDistance = 1.3;
 	float m_HudSize = 1.1;
 	bool m_HudAlwaysVisible = false;
+	bool m_OrdersMenuOpen = false;
 
 	VR() {};
 	VR(Game *game);
@@ -196,6 +201,7 @@ public:
 	void GetViewParameters();
 	void ProcessMenuInput();
 	void ProcessInput();
+	void ProcessOrdersMenuInput();
 	VMatrix VMatrixFromHmdMatrix(const vr::HmdMatrix34_t &hmdMat);
 	vr::HmdMatrix34_t VMatrixToHmdMatrix(const VMatrix &vMat);
 	vr::HmdMatrix34_t GetControllerTipMatrix(vr::ETrackedControllerRole controllerRole);

